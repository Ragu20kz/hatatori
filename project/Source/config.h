#pragma once
#include <DxLib.h>

static const int SCREEN_WIDTH    = 1280;
static const int SCREEN_HEIGHT   = 720;
static const BOOL WINDOW_MODE    = TRUE;
static const char* WINDOW_NAME   = "ŠøŽæ‚è";
static const float WINDOW_EXTEND = 1.0f;

static const int WALL_SIZE = 10;

static const int COLOR_BLACK  = GetColor(0, 0, 0);
static const int COLOR_RED    = GetColor(184, 69, 61);
static const int COLOR_GREEN  = GetColor(62, 187, 65);
static const int COLOR_BLUE   = GetColor(60, 164, 180);
static const int COLOR_YELLOW = GetColor(184, 184, 61);
static const int COLOR_PURPLE = GetColor(120, 68, 193);
static const int COLOR_CYAN   = GetColor(0, 255, 255);
static const int COLOR_WHITE  = GetColor(255, 255, 255);
static const int COLOR_GRAY   = GetColor(178, 178, 178);

static const int PLAYER_NUM = 5;

static const int TERRITORY_SIZE_X = 150;
static const int TERRITORY_SIZE_Y = 200;

static const int ILUST_RADIUS = 38;